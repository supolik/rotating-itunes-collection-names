import RotatingItems from '@/components/RotatingItems';
import Layout from '@/components/ui/Layout';

const Page = () => (
    <Layout>
        <RotatingItems />
    </Layout>
);

export default Page;
